import { postByUrl, getByUrl } from '@/https/apis/general'

/*******
 * @ description:
 * @ param {*} url
 * @ param {*} param
 * @ param {*} callback
 * @ return {*}
 ******/
export function useModalData(url, param, callback) {
  let listData = []

  return { listData }
}
